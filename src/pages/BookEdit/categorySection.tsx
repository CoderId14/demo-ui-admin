import { Row } from 'antd'

function CategorySection() {
  return <Row>{/* <Checkbox.Group options={options} defaultValue={['Apple']} onChange={onChange} /> */}</Row>
}

export default CategorySection
